package com.example;

import java.text.NumberFormat;
import java.util.Arrays;
import java.util.Scanner;
import javax.swing.text.html.HTMLDocument.HTMLReader.HiddenAction;

public class Learning {

    public static void main(String[] args) {

        //// * Arrays
        // int[] numbers = new int[5];
        // numbers[0] = 1;
        // numbers[1] = 2;
        // numbers[2] = 3;

        // System.out.println(numbers);
        // System.out.println(Arrays.toString(numbers));

        // int[] mejorArray = { 4, 5, 6, 7, 8, 9, 10 };

        // System.out.println(mejorArray.length);
        // System.out.println(Arrays.toString(mejorArray));

        //// * Multidimensional Arrays
        // int[][] numbers = new int[2][3];
        // numbers[0][0] = 1;

        // System.out.println(Arrays.toString(numbers));
        // System.out.println(Arrays.deepToString(numbers));

        // int[][] mejorArray = { { 4, 5, 6 }, { 7, 8, 9 } };

        // System.out.println(Arrays.deepToString(mejorArray));

        //// * Constants
        // final float PI = 3.14F; //definitive constant

        //// * Arithmetic Expressions
        // double result = (double) 10 / (double) 3;
        // int x = 1;
        // x++;
        // ++x;

        // int y = ++x;
        // int z = 1;
        // z += 2; // -=, *=, /=

        // System.out.println(result);
        // System.out.println(x);
        // System.out.println(y);
        // System.out.println(z);

        //// * Formating
        // NumberFormat currency = NumberFormat.getCurrencyInstance();
        // String result = currency.format(12334567.891);

        // NumberFormat percent1 = NumberFormat.getPercentInstance();
        // String result1 = percent1.format(0.1);

        // System.out.println(result);
        // System.out.println(result1);

        //// * Comparison Operators
        // int x = 1;
        // int y = 1;
        // System.out.println(x != y);

        //// * Simplifying If Statements
        ////int income = 120_000;
        ////boolean hasHighIncome = false;
        ////if (income > 100_000) {
        ////    hasHighIncome = true;
        ////}

        //int income = 120_000;
        //boolean hasHighIncome = (income > 100_000);

        //// * Ternary Operator
        ////int income = 120_000;
        ////String className;
        ////if (income > 100_000) {
        ////    className = "First";
        ////} else {
        ////    className = "Economy";
        ////}

        //int income = 120_000;
        //String className = income > 100_000 ? "First" : "Economy";

    }

}
